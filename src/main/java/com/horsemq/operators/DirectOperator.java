package com.horsemq.operators;

import com.horsemq.HorseClient;

public class DirectOperator {

    private final HorseClient _client;

    public DirectOperator(HorseClient client) {
        _client = client;
    }

    //todo: send direct message, send request, send response methods
}
